installs: |
  if command -v cargo &> /dev/null; 
    then echo "Already installed rust toolchain.";
    else echo "Installing rust..." && curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh
  fi
