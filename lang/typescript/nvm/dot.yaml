installs: |
  source ~/.zshrc
  if ! command -v nvm &> /dev/null; 
    then echo "Installing nvm..." && sh -c "$(curl -fsSL https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.7/install.sh)";
    else echo "Already installed nvm"; 
  fi
  nvm use v21
