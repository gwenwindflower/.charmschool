installs: |
  if uname | grep -q Darwin
    if type -q qmk
      echo "qmk is already installed!"
    else
      brew tap qmk/qmk
      brew install qmk/qmk/qmk
    end
  else
    echo "Not on macOS, skipping qmk install..."
  end
