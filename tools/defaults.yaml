# Homebrew is installed via bootstrap.sh script to make all this simpler
# and avoid the majority of dots depending on it

installs:
  cmd: |
    if not type -q {{file_name name}}
      brew install {{file_name name}}
    end
