# TODO: only on Darwin, not devcontainers
installs: curl -L https://sw.kovidgoyal.net/kitty/installer.sh | sh /dev/stdin
links:
  kitty.conf: ~/.config/kitty/kitty.conf
  # current-theme.conf: ~/.config/kitty/current-theme.conf
  catppuccin-winnie.conf: ~/.config/kitty/catppuccin-winnie.conf
  quick-access-terminal.conf: ~/.config/kitty/quick-access-terminal.conf
