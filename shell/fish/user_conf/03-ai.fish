# AI tools setup

# Claude Code doesn't use this, but it's helpful for me
# Claude's env var settings are in ~/.claude/settings.json>"env":{}
set -gx CLAUDE_HOME $HOME/.claude

# OpenCode settings
set -gx OPENCODE_ENABLE_EXA 1
