# Dotfiles Git Commit Rules

## Scopes

Use tool scopes across all types, with the exception of Starship, which should be labelled 'prompt'.

**Examples**:

- `feat(nvim):...` (Adding new Neovim plugin)
- `chore(fzf):...` (Updating fzf config)
- `fix(prompt):...` (Starship bug fix)
